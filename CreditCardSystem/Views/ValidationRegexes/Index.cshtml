@model IEnumerable<CreditCardSystem.Models.ValidationRegex>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ValidationRegexString)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ValidationRegexName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValidationRegexString)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValidationRegexName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ValidationRegexId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ValidationRegexId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ValidationRegexId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
